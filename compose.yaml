services:
  mysql:
    image: docker.io/library/mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: reservation_db
      MYSQL_USER: reservation
      MYSQL_PASSWORD: reservation
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-time-zone=+00:00
    ports:
      - "3306:3306"
    volumes:
      - reservations-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 20s

  reservationbackend:
    image: reservationbackend
    build:
      context: .
      dockerfile: ReservationBackend/Dockerfile
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: "Server=mysql;Port=3306;Database=reservation_db;Uid=reservation;Pwd=reservation;Allow User Variables=true;DateTimeKind=Utc"
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy

  reservationfrontend:
    image: reservationfrontend
    build:
      context: ./frontend
    restart: unless-stopped
    environment:
      PORT: "8000"
    ports:
      - "8000:8000"
    depends_on:
      - reservationbackend

volumes:
  reservations-data: